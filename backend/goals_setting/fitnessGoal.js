// duration = distance / 5000 * 1.25 (5000m/speed for normal adult, give 25% more time for quizzes.)

const LEVEL_1_DISTANCE_GOAL = {
    distance: 1000,  // in meters
    duration: 15,    // in minutes
    level: 1
};

const LEVEL_2_DISTANCE_GOAL = {
    distance: 2000,
    duration: 30,
    level: 2
};

const LEVEL_3_DISTANCE_GOAL = {
    distance: 3000,
    duration: 45,
    level: 3
};

const LEVEL_4_DISTANCE_GOAL = {
    distance: 4000,
    duration: 60,
    level: 4
};

const LEVEL_5_DISTANCE_GOAL = {
    distance: 5000,
    duration: 75,
    level: 5
};

const LEVEL_6_DISTANCE_GOAL = {
    distance: 6000,
    duration: 90,
    level: 6
};

const LEVEL_7_DISTANCE_GOAL = {
    distance: 7000,
    duration: 105,
    level: 7
};

const LEVEL_8_DISTANCE_GOAL = {
    distance: 8000,
    duration: 120,
    level: 8
};

module.exports = {
    LEVEL_1_DISTANCE_GOAL,
    LEVEL_2_DISTANCE_GOAL,
    LEVEL_3_DISTANCE_GOAL,
    LEVEL_4_DISTANCE_GOAL,
    LEVEL_5_DISTANCE_GOAL,
    LEVEL_6_DISTANCE_GOAL,
    LEVEL_7_DISTANCE_GOAL,
    LEVEL_8_DISTANCE_GOAL,
};
